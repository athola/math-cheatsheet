\* TLA+ Model Configuration for MagmaModel module
\* This configures the TLC model checker

\* Specify the module to check
MODULE MagmaModel

\* Specification (init and next state relation)
SPECIFICATION
    Init
\* No next-state relation for this static specification

\* Properties to check (invariants)
INVARIANTS
    TypeOK

\* Constants (instantiation)
CONSTANTS
    S = {0, 1}
    op = [x \in {0, 1}, y \in {0, 1} |-> (x + y) % 2]

\* View for state visualization (optional)
VIEW
    []
